interface UpdateTicketRequest {
    name_to?: string,
    description_to?: string,
    quantity_to?: number,
    quantity_available_to?: number,
    price_to?: number
}

export default UpdateTicketRequest;